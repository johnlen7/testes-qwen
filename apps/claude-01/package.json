{
  "name": "orbita-claude-01",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ÓRBITA — site de lançamento imersivo. Frontend puro, sem bibliotecas de UI ou animação.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.1.0",
    "@fontsource-variable/manrope": "^5.1.0"
  }
}
